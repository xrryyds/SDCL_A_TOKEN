# Session Transcripts (v7)

Raw JSONL transcripts of the Qoder sessions that developed v7. Machine format
(not human-readable), but complete — includes every tool call, search, edit, and
debugging step.

| File | Session | Size |
|---|---|---|
| `v7_session_current.jsonl` | v7 实施（改动 A/B/C/D + 验证 + 文档） | 13M |
| `v7_session_previous.jsonl` | v6 设计与实验、论文分析、候选池构建（上下文用尽后续接） | 8.7M |

## 脱敏

所有 GitHub token (`ghp_...`) 已替换为 `ghp_REDACTED`。已确认无真实 token 残留。

## 可读版本

如果只想理解 v7，读 `feature_v7.md` 和 `doc/v7_design.md` 即可，无需翻这些原始转录。
